// import { ProductImage } from "../db/models/image";

// export const processProductImages = (productImages: string[]): ProductImage[] => {
//   return productImages.map((image: string, index: number) => {
//     return {
//       priority: index + 1,
//       link: image
//     };
//   });
// };

// export const processProductSkus = (productsInfo: string[]): ProductImage[] => {
//   return productImages.map((image: string, index: number) => {
//     return {
//       priority: index + 1,
//       link: image
//     };
//   });
// };
